<?php
/**
 * Tests class
 *
 * @package {{packageName}}
 * @since 1.0
 */

declare(strict_types=1);

namespace {{namespace}}\TESTS;

if ( ! defined( 'ABSPATH' ) ) {
	exit; // Exit if accessed directly.
}

/**
 * Placeholder for runtime test helpers.
 *
 * NOTE: Unit/integration tests belong in a top-level `tests/` directory run by
 * PHPUnit, not in autoloaded runtime code. This stub is kept only as a scaffold.
 */
final class Tests {

	/**
	 * Constructor.
	 */
	public function __construct() {}
}
